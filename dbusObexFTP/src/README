DBus ObexFTP Client.

To build the project you can either run make from Debug, or run:

rm marshal.c
rm marshal.h (make sure you don't delete marshal.list)
glib-genmarshal --prefix=marshal marshal.list --header > marshal.h
glib-genmarshal --prefix=marshal marshal.list --body > marshal.c
gcc `pkg-config --libs --cflags glib-2.0 dbus-1 dbus-glib-1` -lobexftp \
    dbus-binding.c marshal.c main.c -o dbusObexFTP
    
