DBus ObexFTP Client.

To build the project you can either run make from Debug, or run:

rm src/marshal.c
rm src/marshal.h (make sure you don't delete marshal.list)
glib-genmarshal --prefix=marshal src/marshal.list --header > src/marshal.h
glib-genmarshal --prefix=marshal src/marshal.list --body > src/marshal.c
gcc `pkg-config --libs --cflags glib-2.0 dbus-1 dbus-glib-1` -lobexftp \
    src/dbus-binding.c src/marshal.c src/main.c -o dbusObexFTP
    
